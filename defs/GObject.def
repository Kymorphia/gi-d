repo GObject-2.0

del union[_Value__data__union]
set record[InitiallyUnownedClass][opaque] 1
set record[ObjectClass][opaque] 1
set record[Value][opaque] 1
set record[WeakRef][opaque] 1

import glib.c.functions
code
{
  abstract class Boxed
  {
    void* ptr;

    this(void* ptr)
    {
      this.ptr = ptr;
    }

    this(Boxed boxed)
    {
      this(g_boxed_copy (getType, boxed.ptr));
    }

    ~this()
    {
      g_boxed_free(getType, ptr);
    }

    GType getType();
  }
}
